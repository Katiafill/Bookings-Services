plugins {
    id 'all-spring-boot-services'
}

dependencies {
    implementation 'org.springframework.cloud:spring-cloud-config-server'
}

bootBuildImage {
    imageName = "bookings-service/config-service"
}