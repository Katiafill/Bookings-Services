plugins {
    id 'all-spring-boot-services'
}

dependencies {
    implementation(libs.starterActuator)
    implementation(libs.starterEurekaServer)
    implementation(libs.starterClientConfig)
}

springBoot {
    mainClass = 'ru.katiafill.bookings.eureka.EurekaServiceApplication'
}