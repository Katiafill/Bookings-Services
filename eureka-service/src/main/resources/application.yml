spring:
  application:
    name: eureka-service
  profiles:
    active: dev
  config:
    import: optional:configserver:${CONFIG_SERVER:http://localhost:8071}
  cloud:
    loadbalancer:
      enabled: false